<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.candra.trainingkopdig">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:name=".di.CoffeeApp"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.TrainingKopdig">
        <activity
            android:name=".activity.MainActivity"
            android:theme="@style/Theme.TrainingKopdig.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.DetailActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.TrainingKopdig.NoActionBar"
            tools:ignore="LockedOrientationActivity" />

        <activity android:name=".activity.MenuMainCoffee"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.TrainingKopdig.NoActionBar"
            tools:ignore="LockedOrientationActivity" />

        <activity android:name=".activity.CartActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.TrainingKopdig.NoActionBar"
            tools:ignore="LockedOrientationActivity" />
    </application>

    <queries> <package android:name="com.whatsapp" /> </queries>

</manifest>